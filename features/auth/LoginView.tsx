"use client";
import { useState } from "react";
import { useAuth } from "./AuthProvider";
export function LoginView({onPublic}:{onPublic:()=>void}){const {signIn}=useAuth();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);return <main className="login-page"><section className="login-card"><div className="brand-mark">CRC</div><span className="eyebrow">CAMPUS CRC</span><h1>Bienvenido a CRC Conecta</h1><p>Ingresa con la cuenta asignada por tu sede.</p><form onSubmit={async event=>{event.preventDefault();setBusy(true);setError("");const message=await signIn(email,password);setError(message??"");setBusy(false)}}><label><span>Correo</span><input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label><span>Contraseña</span><input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="auth-error" role="alert">No fue posible iniciar sesión. Verifica tus datos.</div>}<button className="primary-button wide" disabled={busy}>{busy?"Ingresando…":"Ingresar al Campus"}</button></form><button className="login-public" onClick={onPublic}>Visitar el sitio público →</button></section></main>}
