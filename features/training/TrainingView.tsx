"use client";
import {useEffect,useMemo,useState} from "react";
import {useAuth} from "@/features/auth/AuthProvider";
import {createLesson,createModule,createProgram,loadTraining,type Lesson,type Module,type Program} from "./repository";

export function TrainingView(){const{configured,session}=useAuth();const[organizationId,setOrganizationId]=useState("");const[programs,setPrograms]=useState<Program[]>([]);const[activeId,setActiveId]=useState("");const[expanded,setExpanded]=useState("");const[selectedLesson,setSelectedLesson]=useState<Lesson|null>(null);const[query,setQuery]=useState("");const[modal,setModal]=useState<"program"|"module"|"lesson"|null>(null);const[targetModule,setTargetModule]=useState<Module|null>(null);const[programDraft,setProgramDraft]=useState({title:"",type:"DISCIPLESHIP",description:""});const[moduleDraft,setModuleDraft]=useState({title:"",description:""});const[lessonDraft,setLessonDraft]=useState({title:"",biblicalText:"",centralVerse:"",duration:"45"});const[error,setError]=useState("");const[notice,setNotice]=useState("");const[loading,setLoading]=useState(true);
 const refresh=async()=>{setLoading(true);try{const data=await loadTraining();setOrganizationId(data.organizationId);setPrograms(data.programs);setActiveId(current=>data.programs.some(item=>item.id===current)?current:data.programs[0]?.id??"");setError("")}catch{setError("No fue posible cargar la biblioteca de Formación.")}finally{setLoading(false)}};useEffect(()=>{if(configured&&session)void refresh();else setLoading(false)},[configured,session]);const active=programs.find(item=>item.id===activeId);const filtered=useMemo(()=>programs.filter(item=>item.title.toLocaleLowerCase("es").includes(query.toLocaleLowerCase("es"))),[programs,query]);const totalModules=programs.reduce((sum,item)=>sum+item.modules.length,0);const totalLessons=programs.reduce((sum,item)=>sum+item.modules.reduce((n,module)=>n+module.lessons.length,0),0);
 const done=async(text:string)=>{setModal(null);setNotice(text);await refresh()};const saveProgram=async(e:React.FormEvent)=>{e.preventDefault();try{await createProgram(organizationId,programDraft);await done("Programa guardado en Formación.")}catch{setError("No fue posible crear el programa.")}};const saveModule=async(e:React.FormEvent)=>{e.preventDefault();if(!active)return;try{await createModule(active,moduleDraft);await done("Módulo agregado al programa.")}catch{setError("No fue posible crear el módulo.")}};const saveLesson=async(e:React.FormEvent)=>{e.preventDefault();if(!active||!targetModule)return;try{await createLesson(active,targetModule,lessonDraft);await done("Lección publicada y disponible para los grupos de discipulado.")}catch{setError("No fue posible crear la lección.")}};
 return <div className="content training-content"><div className="module-heading"><div><span className="eyebrow">FORMACIÓN CRC · SUPABASE</span><h1>Programas y lecciones</h1><p>Esta biblioteca alimenta los grupos, las clases y el porcentaje de avance.</p></div><button className="primary-button" disabled={!organizationId} onClick={()=>{setProgramDraft({title:"",type:"DISCIPLESHIP",description:""});setModal("program")}}>＋ Nuevo programa</button></div>{error&&<div className="form-error">{error}</div>}{notice&&<div className="form-success">✓ {notice}</div>}<div className="training-summary"><article><strong>{programs.length}</strong><span>programas</span></article><article><strong>{totalModules}</strong><span>módulos</span></article><article><strong>{totalLessons}</strong><span>lecciones</span></article><div><b>Biblioteca conectada</b><span>Una lección activa cuenta para el avance</span></div></div><section className="training-layout"><aside className="program-list"><label className="module-search"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar programa"/></label>{filtered.map(item=><button key={item.id} className={item.id===activeId?"active":""} onClick={()=>{setActiveId(item.id);setExpanded(item.modules[0]?.id??"");setSelectedLesson(item.modules[0]?.lessons[0]??null)}}><span>{item.type[0]}</span><div><strong>{item.title}</strong><small>{item.type} · {item.modules.length} módulos</small></div><b>›</b></button>)}{!loading&&!filtered.length&&<div className="training-empty"><strong>Sin programas</strong><span>Crea un programa de tipo Discipulado.</span></div>}</aside><section className="curriculum-panel">{active?<><header><div><span className="panel-kicker">{active.type}</span><h2>{active.title}</h2><p>{active.description||"Sin descripción"}</p></div><button className="secondary-button" onClick={()=>{setModuleDraft({title:"",description:""});setModal("module")}}>＋ Módulo</button></header><div className="module-stack">{active.modules.map((module,index)=><article key={module.id} className={expanded===module.id?"open":""}><button className="module-row" onClick={()=>setExpanded(expanded===module.id?"":module.id)}><span>{String(index+1).padStart(2,"0")}</span><div><strong>{module.title}</strong><small>{module.description} · {module.lessons.length} lecciones</small></div><b>{expanded===module.id?"−":"＋"}</b></button>{expanded===module.id&&<div className="lesson-list">{module.lessons.map((lesson,i)=><button key={lesson.id} className={selectedLesson?.id===lesson.id?"selected":""} onClick={()=>setSelectedLesson(lesson)}><i>{i+1}</i><div><strong>{lesson.title}</strong><small>{lesson.biblicalText||"Sin texto bíblico"} · {lesson.duration??"—"} min</small></div><span className={lesson.active?"lesson-active":"lesson-draft"}>{lesson.active?"Activa":"Borrador"}</span></button>)}<button className="add-lesson" onClick={()=>{setTargetModule(module);setLessonDraft({title:"",biblicalText:"",centralVerse:"",duration:"45"});setModal("lesson")}}>＋ Agregar lección</button></div>}</article>)}</div>{!active.modules.length&&<div className="curriculum-empty"><strong>Aún no hay módulos</strong><button className="primary-button" onClick={()=>setModal("module")}>Crear módulo</button></div>}</>:<div className="curriculum-empty"><strong>Selecciona o crea un programa</strong></div>}</section><aside className="lesson-preview">{selectedLesson?<><span className="panel-kicker">LECCIÓN REAL</span><h2>{selectedLesson.title}</h2><div className="lesson-facts"><p><span>Texto bíblico</span><strong>{selectedLesson.biblicalText||"No registrado"}</strong></p><p><span>Versículo central</span><strong>{selectedLesson.centralVerse||"No registrado"}</strong></p><p><span>Duración</span><strong>{selectedLesson.duration??"—"} minutos</strong></p></div><div className="resource-list"><strong>Uso en Discipulado</strong><span>Disponible para programar clases</span><span>Cuenta en el total del programa</span><span>Actualiza el avance al completarse</span></div></>:<div className="training-empty"><strong>Selecciona una lección</strong></div>}</aside></section>
 {modal&&<div className="modal-layer"><button className="modal-scrim" onClick={()=>setModal(null)} aria-label="Cerrar"/><section className="person-modal"><div className="modal-head"><div><span className="eyebrow">FORMACIÓN</span><h2>{modal==="program"?"Nuevo programa":modal==="module"?"Nuevo módulo":"Nueva lección"}</h2></div><button onClick={()=>setModal(null)}>×</button></div>{modal==="program"?<form onSubmit={saveProgram}><Grid><Field label="Nombre"><input required minLength={3} value={programDraft.title} onChange={e=>setProgramDraft({...programDraft,title:e.target.value})}/></Field><Field label="Tipo"><select value={programDraft.type} onChange={e=>setProgramDraft({...programDraft,type:e.target.value})}><option value="DISCIPLESHIP">Discipulado</option><option value="COURSE">Curso</option><option value="SCHOOL">Escuela</option><option value="SEMINAR">Seminario</option><option value="DIPLOMA">Diplomado</option><option value="WORKSHOP">Taller</option></select></Field><Field label="Descripción"><input value={programDraft.description} onChange={e=>setProgramDraft({...programDraft,description:e.target.value})}/></Field></Grid><Save label="Crear programa"/></form>:modal==="module"?<form onSubmit={saveModule}><Grid><Field label="Nombre del módulo"><input required minLength={3} value={moduleDraft.title} onChange={e=>setModuleDraft({...moduleDraft,title:e.target.value})}/></Field><Field label="Descripción"><input value={moduleDraft.description} onChange={e=>setModuleDraft({...moduleDraft,description:e.target.value})}/></Field></Grid><Save label="Crear módulo"/></form>:<form onSubmit={saveLesson}><Grid><Field label="Título"><input required minLength={3} value={lessonDraft.title} onChange={e=>setLessonDraft({...lessonDraft,title:e.target.value})}/></Field><Field label="Texto bíblico"><input value={lessonDraft.biblicalText} onChange={e=>setLessonDraft({...lessonDraft,biblicalText:e.target.value})}/></Field><Field label="Versículo central"><input value={lessonDraft.centralVerse} onChange={e=>setLessonDraft({...lessonDraft,centralVerse:e.target.value})}/></Field><Field label="Duración (minutos)"><input type="number" min="1" max="1440" value={lessonDraft.duration} onChange={e=>setLessonDraft({...lessonDraft,duration:e.target.value})}/></Field></Grid><Save label="Publicar lección"/></form>}</section></div>}</div>}
function Grid({children}:{children:React.ReactNode}){return <div className="form-grid">{children}</div>}function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="form-field"><span>{label}</span>{children}</label>}function Save({label}:{label:string}){return <div className="modal-actions"><button className="primary-button" type="submit">{label}</button></div>}
